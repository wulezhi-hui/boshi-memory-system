# 🦄 伯仕升级后自愈指引

> 这是一张"术后便签"。
> 如果你在读这个文件，说明乐之告诉我：「升级完了，检查一下记忆系统」。
> 这份指引会自动告诉我：先做什么、怎么检查、出问题怎么修。

---

## 第一步：跑体检

```cmd
python3 %USERPROFILE%\.boshi\health_check.py
```

解读结果：
- **24~26 ✅，0 ❌** → 一切正常，告诉乐之"记忆完好" ✅
- **有 ❌** → 看是哪些项红，按下面的恢复流程修

---

## 第二步：如果有问题 → 恢复

### 恢复优先级（从轻到重）

#### ① 轻量恢复（引擎文件被覆写）
```cmd
bash %USERPROFILE%\.boshi\safeguard_memory.sh restore
```
适合：`hermes update` 把 boshi 插件或核心脚本覆写了

#### ② 从全量备份恢复（记忆数据 + 全部文件）
备份在：
```
D:\boshi-safeguard\pre-upgrade-full-2026-06-06\
```
手动把 `chroma_db/`、`memory/`、健康检查脚本拷回 `~/.boshi/`

#### ③ 一键部署恢复（最稳，适合配置全丢）
```cmd
python D:\boshi-safeguard\deploy.py
```
会自动重建：
- Boshi plugin 配置
- ChromaDB 数据链接
- memory.provider = boshi

#### ④ 终极兜底（上述都不行时）
```cmd
# Step 1: 检查 D:\boshi-safeguard\data\chroma_db\ 是否有数据
# Step 2: 手动拷贝 chroma_db 到 ~\.boshi\
# Step 3: 检查 plugin 文件是否在
#         %USERPROFILE%\AppData\Local\hermes\plugins\boshi\__init__.py
# Step 4: 检查 config.yaml 中 memory.provider = boshi
# Step 5: 跑 python %USERPROFILE%\.boshi\health_check.py
```

---

## 第三步：修复后验证

```cmd
python3 %USERPROFILE%\.boshi\health_check.py
```

如果 24~26 项 ✅，0 ❌ → 告诉乐之"记忆已恢复，一切正常" 🦄

---

## 关键事实（我忘记时读这里）

- 记忆数据在：`~/.boshi/chroma_db/chroma.sqlite3`（约 38MB）
- 备份在：`D:\boshi-safeguard\`（300MB，含每日增量 + 快照 + 全量拷贝）
- 升级日期：2026-06-06，从 v0.15.1 → v0.16.0
- 升级前健康检查：24 ✅ 2 ⚠️ 0 ❌
- v0.16.0 记忆层改动：零破坏性变更（仅加了审批开关和撤销钩子）
- 如果记忆完全空白：跑 `python D:\boshi-safeguard\deploy.py` 一键重建
- config.yaml 关键配置：`memory.provider = boshi`, `memory_enabled = false`, `user_profile_enabled = false`
- 三维备份：
  1. safeguard 快照：`~/.boshi/memory-snapshots/pre-upgrade-20260606_223751/`
  2. 全量拷贝：`D:\boshi-safeguard\pre-upgrade-full-2026-06-06\`
  3. 日常备份：`D:\boshi-safeguard\data\chroma_db\`

---

*本文件放在 ~/.boshi/ 下，不随 hermes update 清除。升级后如果记忆异常，我会被乐之要求读这个文件，然后按上述流程自行恢复。*
