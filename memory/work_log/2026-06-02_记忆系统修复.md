# 🔧 伯仕工作日志 — 记忆系统修复

## 基本信息

| 项目 | 内容 |
|:----|:------|
| **日期** | 2026-06-02 |
| **机器** | 本机（笔记本/伯仕） |
| **操作人** | 伯仕（笔记本端） |
| **原因** | 对话数据未实时写入 chroma 记忆库，auto_capture 自5月24日后未运行 |
| **症状** | chroma 库有 10,587 条记录（5/30~6/1），但6月2日对话一条未存 |

## 诊断过程

1. 检查 auto_capture 状态 → 最后一次运行 `2026-05-24 17:56`，之后未启动
2. 检查 boshi 插件 → 存在于 `plugins/boshi/`，但未在 config 中注册为 memory provider
3. 检查 config.yaml → 缺少 `memory:` 段，`provider: boshi` 未设置
4. 验证 chroma 数据库 → 文件完整（39MB），查询正常，10,587 条

## 修复措施

| 步骤 | 修改内容 | 目的 |
|:----|:---------|:-----|
| 1 | `config.yaml` 末尾追加 `memory:` 段 | 配置 memory provider |
| 2 | 设置 `provider: boshi` | 激活 boshi 记忆插件 |
| 3 | 复制 `plugins/boshi/` 到 `~/.hermes/plugins/` | 确保插件可被发现 |
| 4 | 待办：重启 Gateway 使配置生效 | 激活实时写入 |

## 当前状态

```
config.yaml memory 段配置完成：
  memory_enabled: true
  user_profile_enabled: true
  provider: boshi

boshi 插件位置：
  ~/.hermes/plugins/boshi/__init__.py  ✅

chroma 数据库：
  ~/.boshi/chroma_db/chromadb.sqlite3（39MB，10587条）✅
```

## 后续需关注

- Gateway 重启后 boshi 插件应自动载入，对话实时写入
- auto_capture 仍需定时运行作为兜底
- 建议添加开机自启动 auto_capture

## 参考资料

- chroma_bridge.py 写入路径：`~/.boshi/chroma_db`
- tiered_memory.py BOSHI_HOME：`~/.boshi`
- boshi 插件类：`BoshiMemoryProvider(MemoryProvider)`
