# 伯仕记忆系统 — 工作日志（2026-05 至 2026-08）

> 正本位置：C:/Users/Administrator/.boshi/docs/伯仕工作日志.md
> 桌面副本：C:/Users/Administrator/Desktop/伯仕技术文档/伯仕工作日志.md
> 全文索引：ChromaDB 中 type=work_log / type=project_log，通过 boshi_search 检索
> ---
>
> **铁律（2026-08-18 立）：记忆系统的任何改动、升级、迭代，都必须记录在工作日志 + 技术文档。**

## 2026-08-19 — 知识图谱完善：召回/动态实体/去重/回填

**操作：** feature（完善知识图谱，使其正常发挥作用）
**文件：** knowledge_graph.py、chroma_bridge.py、boshi_core.py（~/.boshi 与 projects 仓库同步）
**详情：**
- **背景**：图谱此前恒为 0（见上一条 fix 记录），修复基础后继续完善 5 项：
  1. **补 `KnowledgeGraph.search()` 方法** — 图谱召回核心，`_graph_search` 依赖它；按查询词匹配实体名+已知实体兜底。此前缺失导致三路融合 graph=0
  2. **动态实体学习 `learn_entities()`** — 从内容自动发现新实体并扩展 KNOWN_ENTITIES（英文大写词/《书名号》/中文"XX系统"后缀）；修复中英边界 `\b` 不生效问题（用 `(?<![A-Za-z0-9])`）；新增 `_COMMON_EN_WORDS` + `_KEEP_ACRONYMS` 噪音过滤（排除常见英文词/无意义缩写/句子片段）
  3. **边类型区分** — auto_link_entities 按上下文线索词判断：derives(依赖/使用/连接)/updates(升级/更新)/extends(扩展/集成)/related(共同出现)，reason 更具体
  4. **去重修复** — 原 `get_relations(top_k=50)` 导致边多时去重失效（重复边暴涨）；改为模块级 `_EDGE_CACHE` 集合 O(1) 去重；`get_all_relations()` 精确查询（不走语义搜索，避免误匹配）
  5. **stats() 低估修复** — 原 `search_memory("", top_k=10000)` 空查询受限只返回 20 条；改用 `get_all_relations()` 精确统计
- **批量回填**：用 150 条最新记忆建真实图谱 → 794 边 / 225 节点（150 秒，去重修复前 500 条 20 分钟且全重复）
- **验证**：三路融合召回 graph=3；graph_query('伯仕') 返回 18 关联节点 20 边；save() 联动自动建边
- **已知限制**：回填历史数据含少量噪音实体（ImportError 等，从代码/边文本学到）；日常 save() 走对话内容不会再产生；CPU/GPU/API 等缩写作为技术实体保留

## 2026-08-19 — 知识图谱修复 + save() 联动（图谱从0激活）

**操作：** fix（功能修复 + 接线）
**文件：** boshi_core.py、knowledge_graph.py（~/.boshi 与 projects 仓库三处同步）
**详情：**
- **问题**：知识图谱恒为 0 节点 0 边。三层根因：
  1. `KnowledgeGraph` 类从未实现——boshi_core 的 `_get_kg()` 一直 ImportError（`cannot import name 'KnowledgeGraph'`）
  2. `status()` 里 `except Exception: pass` 吞掉错误 → 图谱静默显示 0
  3. `save()` 从不调用图谱联动 → 新记忆从不建边
- **修复**：
  1. `knowledge_graph.py` 末尾新增 `KnowledgeGraph` 类（桥接纯函数版：stats/query/add_node/add_edge）
     - stats()：统计 ChromaDB 中 type=relation 边数 + 实体节点数
     - query()：从实体 BFS 返回关联边
     - add_edge()：委托 add_relation
  2. `boshi_core.save()` 接入 `auto_link_entities(content)` → 每次存记忆自动提取已知实体（KNOWN_ENTITIES 40+个）并建 related 边；失败不阻断主流程
- **验证**：save() 后自动建边（`乐之--related-->伯仕`、`DeepSeek--related-->Ollama`）；status() 图谱 nodes=4 edges=2；graph_query/graph_add_edge 全部正常
- **历史节点**：graph/nodes.json 的 6 个节点是 2026-06-08 一次性脚本残留，非活系统产物

## 2026-08-18 — 纠错：bge-m3 同步到开发仓库（修复版本漂移）

**操作：** fix（纠错）
**背景：** 排查"hybrid_search 报 Collection expecting 1024 got 384"时发现——该错误是**测试路径 sys.path 顺序污染导致的假警报**，生产路径（~/.boshi/）一直正常使用 bge-m3。
**根因：** 版本漂移——开发仓库 `projects/boshi-memory-system/` 的 onnx_embed.py / boshi_core.py 停留在旧版（all-MiniLM-L6-v2，384维），未同步运行时 `~/.boshi/` 的 bge-m3 升级（1024维）。任何人从 projects/ 目录 import 就会用 384 维模型查 1024 维 collection → 报错。
**纠正动作：**
- 备份旧版 → memory-snapshots/pre-bge3-sync-20260818/（onnx_embed.py + boshi_core.py）
- 同步运行时新版 → 仓库：onnx_embed.py（bge-m3 1024维）、boshi_core.py（hybrid_search 返回 sessions）、install.py、download_model.py（补充缺失）
- 验证：从 projects/ 路径加载 chroma_bridge → ef.name()=bge-m3-onnx，search 正常（results+state.db sessions 均命中）
**结论：** bge-m3 早已集成在记忆系统（本地 ONNX，1024维，零 Ollama 依赖），无需"重新向量化"。之前文档中"384 维向量，兼容现有 collection"是错误假设，现已在运行时纠正为 bge-m3。
**文件：** onnx_embed.py、boshi_core.py、install.py、download_model.py、memory-snapshots/pre-bge3-sync-20260818/


## 2026-08-18 — sync_turn 记录助手结论（assistant_conclusion）

**操作：** feature
**文件：** plugins/boshi/__init__.py（Hermes Memory Provider 插件）
**详情：**
- 原行为：sync_turn 每轮只记录用户消息（topic=conversation，≤500 字符），assistant_content 参数被忽略
- 新增：记录助手结论（topic=assistant_conclusion，≤800 字符）——即 agent 输出到屏幕的最终答复
  - 设计原则：思考推理过程不记，但推理的成果（结论/交付说明）是记忆价值所在
- 新增 `_is_capture_worthy_conclusion()` 过滤函数：
  - 跳过纯工具回显（JSON 开头 / ```代码块 / exit_code / Tool result / 工具结果 / 已执行）
  - 跳过超短无标点噪音（OK / done / 已好，<12 字符且无标点）
  - 短结论（如"已修复。"）保留——不套用 is_trivial_prompt（该过滤器会误杀精炼结论）
- 验证：8/8 单元测试通过 + 真机写入 ChromaDB 成功（topic/role 正确标记，可检索）
- 生效：Hermes 网关重启后每轮对话的助手结论自动入库


## 2026-06-18 — v6.1 向量层重构（ONNX 替代 SentenceTransformer，零外部依赖）

**操作：** upgrade
**文件：** chroma_bridge.py、onnx_embed.py、install.sh、install.sh
**详情：**
- Embedding 引擎从 `SentenceTransformer(paraphrase-multilingual-MiniLM-L12-v2)` 重构为 ChromaDB 内置 `ONNXMiniLM_L6_V2`
  - 不再依赖：`torch` (~1.5GB)、`transformers`、`sentence-transformers`、`HuggingFace` 缓存
  - 保留依赖：仅需 `chromadb`（已内含 `onnxruntime` + `tokenizers`）
  - 向量模型：`all-MiniLM-L6-v2`（384 维，ONNX 格式）
  - 仓库自带版本：`models/all-MiniLM-L6-v2/onnx/`（model.onnx + tokenizer + vocab，~87MB）
  - 加载策略：优先用仓库自带 → 缺失时 fallback 到 ChromaDB 自动下载（需联网）
- 新增 `onnx_embed.py`：封装 `BoshiEmbeddingFunction`，处理模型路径探测和 monkey-patch
- 修改 `chroma_bridge.py`：
  - 删除：`SentenceTransformerEmbeddingFunction`、`LOCAL_MODEL_PATH`（HuggingFace 缓存路径）
  - 替换：`from onnx_embed import get_embedding_function as _get_embedding_function`
- 修改 `install.sh`：
  - Ubuntu 一键安装脚本改为方案 B：仓库不打包 86MB ONNX 模型
  - 安装时自动触发 ChromaDB 内置下载机制（首次实例化 `ONNXMiniLM_L6_V2`）
  - 或从 `~/.cache/chroma/onnx_models/` 复制到 `~/.boshi/models/`
- GitHub 仓库推送：
  - 核心代码：onnx_embed.py、chroma_bridge.py、install.sh
  - 仓库体积：不含 model.onnx，用户 clone 后即拥有代码（模型按需下载）
- 兼容性：向量维度仍为 384，现有 ChromaDB collection 数据无损，无需重建
- 历史脉络：
  - v4.0：BGE-M3 (1024维, Ollama 依赖) → v5.0：ONNX (外网下载) → v5.3：all-MiniLM-L6-v2 (HuggingFace 缓存) → **v6.1：ONNX 自带/自动下载 (零重型依赖)**

## 2026-06-13 — v6.0 集成修复（sys.path + plugin sync_turn）

**操作：** fix
**文件：** plugins/boshi/__init__.py、memory/knowledge_graph.py、memory/user_profile.py
**详情：**
- 发现 v6.0 五大功能（auto_link_entities、update_profile、hybrid_search、isLatest、auto_forget）从未集成到 plugin 入口 __init__.py
- 根因：测试脚本 _test_sm.py 绕过了 sys.path 冲突（注释写明"不要加 MEMORY_DIR"），但生产代码 __init__.py 和 boshi_core.py 把 memory/ 放在 sys.path 前面，导致旧版覆盖新版
- 修复措施：
  1. 将 v6.0 版 knowledge_graph.py（14KB）和 user_profile.py（10KB）复制到 memory/ 目录覆盖旧版（旧版备份到 memory-snapshots/pre-v60-integration-20260613/）
  2. sync_turn 集成 v6.0 五大功能：
     - 写入时标记 isLatest=True（版本链）
     - knowledge_graph.auto_link_entities 替代内联实体提取（步骤5）
     - user_profile.update_profile 自动画像维护（步骤6）
     - hybrid_search 替代纯语义搜索（queue_prefetch 步骤4）
     - auto_forget 在热度衰减时自动执行（queue_prefetch 步骤0）
  3. queue_prefetch 新增 knowledge_graph.get_graph_context 图谱关联检索
- 测试：_test_sm.py 28/28 ✅，_test_p2_fast.py 19/19 ✅

## 2026-06-12 — MCP Server + CLI 双轨开放接口

**操作：** feature
**文件：** boshi_core.py (新增)、boshi_mcp_server.py (新增)、boshi_cli.py (新增)
**详情：**
- 新增三个文件，全部放在 `~/.boshi/`，不改动6.0任何原有代码
- `boshi_core.py` — 共享底层Core层，惰性导入chroma_bridge和knowledge_graph，暴露统一API
- `boshi_mcp_server.py` — MCP Server，8个tool（search/save/delete/status/profile/graph/graph-add/recent），mcp SDK 1.27+，stdio协议
- `boshi_cli.py` — CLI，7个子命令（search/save/delete/status/profile/graph/recent/brief）
- 修复mcp SDK装饰器兼容问题：`@server.list_tools` → `@server.list_tools()`，`@server.call_tool`同理（1.27+中装饰器工厂需加括号调用）
- CLI测试通过：status✅ search✅(vector/hybrid/graph/all) save✅ profile⚠️(部分空) recent⚠️(content空-数据层问题) graph✅ graph-add-node✅
- MCP Server测试通过：initialize✅ tools/list(8个)✅ tools/call(boshi_status)✅
- 设计原理：MCP给agent用（Claude Code/Cursor/Hermes），CLI给人/脚本用，两者通过boshi_core共享同一底层，不重复实现业务逻辑
- Hermes配置方式：config.yaml → mcp_servers.boshi → command:python args:["~/.boshi/boshi_mcp_server.py"]

## 2026-06-11 — Supermemory 借鉴项目 P0-P2 全部完成

**操作：** Supermemory 借鉴项目 P0-P2 全部完成
**详情：**
- P0③ 版本链/不可变日志：search_memory(all_versions) + deprecate_memory + update_memory
- P0① 自动事实提取增强：extract_facts 三层提取（60+关键词 + 消息结构正则）
- P1② 用户画像层：user_profile.py 新建（Static+Dynamic 自动维护 + 10分钟缓存）
- P1④ 混合搜索：hybrid_search() 语义+全文合一
- P2⑤ 矛盾消解+自动遗忘：auto_forget + detect_conflicts + resolve_conflict
- P2⑥ 知识图谱：knowledge_graph.py 新建（updates/extends/derives/related 四种关系）
- 完整流程：Ruff 扫描→独立审查→功能测试→修复验证，29/29 通过

## 2026-06-11 — CN Desktop 污染修复 + ChromaDB 重建

**操作：** fix
**详情：**
- CN Desktop 污染导致 metadata 损坏：10,587 条缺 isLatest，2,101 条缺 type
- 修复步骤：① dry_run 摸底 ② 备份 chroma_db ③ 补齐 isLatest+type ④ 重建 ChromaDB collection（11,184条全量导出→删除→回灌）
- 结果：where 过滤全部恢复（type=fact / $and / isLatest 三项通过）
- 备份：chroma_db_backup_20260611_191858

## 2026-06-11 — R18 代码审查测试流程建立

**操作：** skill
**详情：**
- 写入 boshi-work-rules 技能：Ruff 扫描→独立审查（delegate_task）→功能测试→修复验证
- 沉淀自 Supermemory 项目实践：审查抓出 4 个 bug，测试发现 ChromaDB 底层污染
- 以后所有 .py 修改必须走完整流程

## 2026-06-10 — ChromaDB 索引修复 + hot.json 清理 + 桌面文档同步

**操作：** fix
**文件：** chroma_db（重建）、hot.json（冻结）、桌面文档（同步）
**详情：**
- ChromaDB query() 返回 documents=null 的根因：metadata 字段名被时间戳 float 覆盖（10587条中有8227条损坏）
- 修复方式：用 ChromaDB API 逐批读取数据 → 清理损坏 metadata → 重建新 collection
- 结果：10587/10587 条全量恢复，搜索正常
- hot.json 冻结为 .migrated，hot.json.bak 删除
- 桌面"伯仕技术文档"新增 v5.7_技术架构文档.md

## 2026-06-09 — Hermes 升级恢复

**操作：** fix
**结果：** tiered_memory.py 恢复到 v3.0，v4.0 实验性改动归档到 changes_20260609_v4_attempt/

## 2026-06-01 — hot.json 彻底迁移 + 内置记忆关闭

**操作：** migration
**详情：**
- _save_hot() 改为批量写入 ChromaDB（1446条，23秒）
- hot.json 冻结为 hot.json.migrated
- config.yaml 设置 memory_enabled=false, user_profile_enabled=false
- 三层自愈架构部署（L1 config保护/L2 插件文件保护/L3 数据恢复）

## 2026-06-01 — 时间戳归一化修复

**操作：** bug_fix
**详情：**
- chroma_bridge.py 新增 _normalize_metadata() 函数
- 8244条已有数据的字符串时间戳→float 迁移
- SQLite 批量 UPDATE 确保零字符串残留

## 2026-05-30 — ChromaDB Rust 崩溃恢复

**操作：** bug_fix
**详情：**
- 批量 1444 条写入撑爆 HNSW compactor → Rust 段错误
- 分批参数定为 30条/批 + 2秒间隔
- 全量恢复完成 ✅

## 2026-05-26 — v4.0 统一 Chroma 存储

**操作：** feature
**详情：**
- 存储从三套（hot.json + Chroma + cold.json）统一为纯 Chroma
- hot.json 649条迁移到 Chroma
- 新增 conversation_turn 类型兜底写入

## 2026-05-23 — sync_turn 实时写入 + prefetch 扩展到2天

**操作：** feature
**详情：**
- sync_turn 每轮对话后即时写 ChromaDB
- queue_prefetch 跨入口摘要扩展到最近2天
- extract_facts 双路径写入（事实提取 + conversation_turn 兜底）

## 2026-05-23 — 僵尸进程清理

**操作：** cleanup
**详情：**
- 清理 114 个空转进程（bridge_watchdog 看门狗 bug）
- 删除 3 个僵尸 cron
- 清理 hot.json 765条旧数据
