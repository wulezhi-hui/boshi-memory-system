# 伯仕记忆系统 v6.2 架构文档

> 正本：~/.boshi/docs/伯仕记忆系统v6.2_技术架构文档.md
> 上一版：伯仕记忆系统v6.1_技术架构文档.md（2026-06-18）
> 发布日期：2026-08-17

---

## 一、版本概述

| 版本 | v6.2 "双轨接入版" |
|:-----|:-----|
| 发布日期 | 2026-08-17 |
| 上一版本 | v6.1（2026-06-18） |
| 核心变更 | ① 接入方式升级为**双轨**（Hermes MemoryProvider 插件 + MCP Server 并存，可同时启用）② 向量模型升级 bge-m3 ONNX（1024 维）③ MCP 适配 2.0.0 ④ 多 Agent 会话源路由 ⑤ 跨平台一键安装脚本，任何人可安装使用 |

### v6.2 变更明细（相对 v6.1）

| 变更 | 说明 |
|:-----|:-----|
| 🔌 插件方式正式化 | `plugins/boshi/__init__.py`（298 行）实现 Hermes `MemoryProvider` ABC，`memory.provider: boshi` 激活。每轮自动 `prefetch` 召回 + `sync_turn` 存储 + `system_prompt_block` 画像/热区注入，**无需 agent 记得调用工具** |
| 🔗 MCP 方式保留 | `boshi_mcp_server.py` 8 个工具，供 agent 主动调用 / 外部 Agent（Claude Code / Cursor）连接 |
| 🧠 bge-m3 ONNX | 1024 维向量（替代 MiniLM 384 维），本地 CPU 推理零外网依赖；chroma 改用 cosine 空间；修复 search 分数转换与排序 |
| 🔄 MCP 2.0.0 | `MCPServer + @server.tool` 新写法，移除 mcp 1.x 依赖 |
| 👥 多 Agent 会话源 | `session_sources.py` 自动探测 Hermes/DSH 记忆体，按 Agent 路由检索；hybrid 检索合并跨 Agent 结果 |
| 🐛 8 项修复 | extractor 异步化（15s 超时+规则兜底）、chroma 单例客户端、FTS5 全文检索（trigram 中文 MATCH + LIKE 降级）、add_relation 去重、keyword_search 分批、路径硬编码修复等 |
| 📦 一键安装 | `install.sh`（Linux/macOS curl\|bash）+ `install.py`（Windows）+ `download_model.py`（bge-m3 模型下载，默认 hf-mirror 国内镜像，断点续传） |
| 🔧 工程规范 | `.gitattributes` 统一 LF 行尾；移除泄露凭据；`.gitignore` 排除运行时数据（chroma_db/venv/models/） |

---

## 二、总体架构（双轨接入）

```
┌──────────────────────────────────────────────────────────┐
│                  外部 Agent / 人工终端                        │
│  ├─ Hermes Agent（插件方式）★ 每轮自动召回/存储/画像注入        │
│  ├─ Claude Code / Cursor / Hermes（MCP 方式）8 个工具        │
│  └─ 终端脚本 / 人工操作（CLI）9 个子命令                        │
├──────────────────────────────────────────────────────────┤
│  开放接口层（v6.1 → v6.2 双轨 ★）                            │
│  ├─ plugins/boshi/__init__.py ★ 插件 MemoryProvider ABC     │
│  ├─ boshi_mcp_server.py      MCP Server 8 tools（mcp 2.0）  │
│  ├─ boshi_cli.py             CLI 子命令                      │
│  └─ boshi_core.py            共享底层（惰性导入桥接层）         │
├──────────────────────────────────────────────────────────┤
│                  Hermes Agent 对话层                          │
├──────────────────────────────────────────────────────────┤
│  记忆引擎层                                                 │
│  ├─ session_sources.py ★ 多 Agent 会话源路由                 │
│  ├─ user_profile.py        Static+Dynamic 用户画像          │
│  ├─ knowledge_graph.py     实体 + 4 种关系 + 版本追踪         │
│  └─ extract_facts 异步化    15s 超时 + 规则兜底               │
├──────────────────────────────────────────────────────────┤
│  存储层（chroma_bridge.py）                                  │
│  ├─ ChromaDB（bge-m3 ONNX 1024 维，cosine 空间）            │
│  ├─ 版本链/不可变日志     追加不覆盖 + isLatest               │
│  ├─ 混合搜索             hybrid_search（语义 + FTS5 全文）   │
│  ├─ 自动遗忘             热度衰减 + 时间过期折旧              │
│  └─ 全量会话             state.db SQLite FTS5（trigram）    │
└──────────────────────────────────────────────────────────┘
```

---

## 三、双轨接入详解

### 3.1 插件方式（Hermes MemoryProvider）★ v6.2 新增

- **文件**：`plugins/boshi/__init__.py` → 复制到 `$HERMES_HOME/plugins/boshi/`
- **激活**：`config.yaml` → `memory.provider: boshi`
- **机制**：实现 `agent/memory_provider.py` 的 `MemoryProvider` ABC，由 Hermes MemoryManager 生命周期驱动：

| 生命周期 | 行为 |
|:-----|:-----|
| `system_prompt_block()` | 注入记忆库状态 + 用户画像热区（会话组装一次） |
| `queue_prefetch()` / `prefetch()` | 每轮后台召回 bge-m3 语义 + 图谱融合，注入上下文 |
| `sync_turn()` | 每轮对话后自动把用户消息存入记忆库 |
| `on_memory_write()` | 镜像：内置 memory 工具写入时同步存伯仕 |
| `on_pre_compress()` | 压缩前保存即将丢弃的对话 |
| `backup_paths()` | 声明 chroma_db/ 纳入 `hermes backup` |

- **与 MCP 的关系**：可同时启用。插件负责"被动自动"（每轮注入），MCP 负责"主动调用"（agent 按需搜索/管理）。

### 3.2 MCP 方式（8 工具）

| 工具 | 作用 |
|:-----|:-----|
| `boshi_search` | 语义 + FTS5 混合检索 |
| `boshi_save` | 存储记忆 |
| `boshi_delete` | 删除记忆 |
| `boshi_status` | 记忆库状态 |
| `boshi_profile` | 用户画像 |
| `boshi_graph` | 知识图谱查询 |
| `boshi_graph_add` | 图谱添加节点/关系 |
| `boshi_recent` | 最近 N 条记忆 |

### 3.3 DSH 方式（自动记忆插件）★ 2026-08-25 新增

让 DSH（DeepSeek Harness）也获得与 Hermes 插件模式等价的自动记忆能力，无需 agent 记得调用工具。

| 文件 | 作用 |
|:-----|:-----|
| `boshi_bridge.py` | 桥接层：输出干净 JSON（save/search/profile/status），供 DSH 插件 subprocess 解析 |
| `dsh/boshi-auto-memory.mjs` | ESM Cordis 插件：监听 `agent/inbox/claimed` 异步存储 + 召回，`systemPrompt.section()` 动态注入画像/召回 |
| `dsh/README.md` | DSH 部署说明 |

- 机制：插件通过 DSH `subprocess` 异步调用 `boshi_bridge.py`（fire-and-forget，不阻塞对话），结果写内存缓存；`systemPrompt.section()` 的 text provider 每次组装时同步读缓存注入
- 三件套：存储对话（过滤纯标点/≤2字符零散输入）、画像注入、召回注入（异步，下一轮生效）
- 与 MCP 方式互补：MCP 是主动按需检索，DSH 插件是被动自动记忆，可同时启用

---

## 四、向量层（bge-m3 ONNX）★ v6.2 升级

| 项 | v6.1 | v6.2 |
|:-----|:-----|:-----|
| 模型 | SentenceTransformer MiniLM-L12-v2 | **BAAI/bge-m3（Xenova ONNX 导出）** |
| 维度 | 384 | **1024** |
| 文件 | 外部依赖 | `~/.boshi/models/bge-m3/onnx/model_quantized.onnx`（int8 ~569MB） |
| 推理 | sentence-transformers | onnxruntime CPU（零外网依赖） |
| 空间 | L2 | **cosine** |
| 下载 | 无 | `python download_model.py`（hf-mirror 国内镜像，断点续传） |

- `onnx_embed.py`：onnxruntime + transformers tokenizer 推理封装；`model_quantized.onnx` 优先，`model.onnx`（fp32）回退
- `chroma_bridge.py` 通过 `get_embedding_function()` 统一取 embedding，MCP/CLI/插件共享

---

## 五、多 Agent 会话源（session_sources.py）★ v6.2 新增

- 自动探测 Hermes / DSH 记忆体，按 Agent 路由检索
- hybrid 检索合并跨 Agent 会话结果，一次搜索同时覆盖多个入口的会话记录

---

## 六、部署与安装

### 6.1 一键安装（其它人可直接使用）

```bash
# Linux / macOS（自动下载 bge-m3 模型，默认 hf-mirror 国内镜像）
curl -sL https://raw.githubusercontent.com/wulezhi-hui/boshi-memory-system/main/install.sh | bash

# Windows
git clone https://github.com/wulezhi-hui/boshi-memory-system.git %USERPROFILE%\.boshi
python %USERPROFILE%\.boshi\install.py
```

安装内容：部署代码 → 安装依赖（chromadb/mcp>=2.0/onnxruntime/transformers）→ 下载模型 → 复制插件 → 写双轨配置 → 安装 skill。

### 6.2 环境要求

| 组件 | 要求 |
|:-----|:-----|
| Python | 3.10+（Windows 3.11 验证） |
| 依赖 | chromadb / mcp>=2.0.0 / onnxruntime / transformers / pyyaml |
| 模型 | bge-m3 ONNX ~569MB（首次安装自动下载） |
| 数据目录 | `~/.boshi/`（代码 + chroma_db + models） |

---

## 七、已知注意点

- 路径统一 `~/.boshi`（不再硬编码 Administrator）
- 实体提取默认走 Ollama LLM，对话中请用 `extract_facts_async`（后台线程 + 15s 超时）避免阻塞
- 检索优先 FTS5（trigram 中文 MATCH），失败自动降级 LIKE
- 插件方式依赖 Hermes `agent/memory_provider.py` 的 `MemoryProvider` ABC（Hermes v6.2+ 原生支持，无需 run_agent.py 补丁）
- Hermes 升级可能覆盖 `mcp_tool.py` 的 isError 兼容补丁（见 docs/Hermes集成技术手册.md §7.1）
- **ChromaDB HNSW 索引损坏的识别与恢复**（2026-08-25 实战）：
  - 症状：所有读操作（count/peek/get）触发 Rust 组件 access violation（段错误），MCP 连接反复 `Connection closed`
  - 诊断：SQLite `PRAGMA integrity_check` 通过（数据未丢）→ 新建 collection 正常（排除版本问题）→ `index_metadata.pickle` 中 `dimensionality=None`/`max_seq_id=None`/`id_to_seq_id` 空
  - 恢复：从 SQLite 提取文档+metadata → 用 bge-m3 重算向量 → 按 token 长度排序分桶重建（batch 200）→ 替换 `chroma_db/`
  - 预防：HNSW 索引元数据损坏多与进程异常终止（如 MCP 反复强杀）有关，建议依赖 `backup_chroma.py --snapshot` 定期快照
